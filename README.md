# vintage-car-club-management

The application uses:
-> Spring Boot
-> Java 17
-> Maven
-> PostgreSQL
-> H2 in-memory database
-> MockMVC (for integration tests)
-> JUnit (for unit tests)
-> Mockito (to mock dependencies for unit tests)
